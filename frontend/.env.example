# Base URL for the FastAPI backend (no trailing slash).
VITE_API_BASE=http://localhost:8000

# Optional tenant id, sent as the X-Tenant-Id header on POST /ask.
# Leave unset for single-tenant deployments.
# VITE_TENANT_ID=acme
