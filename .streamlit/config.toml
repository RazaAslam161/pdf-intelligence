# Production-oriented Streamlit configuration.
# Keep maxUploadSize >= MAX_UPLOAD_MB in .env so the app-level per-file check stays the binding limit.

[server]
maxUploadSize = 25
headless = true
enableXsrfProtection = true
enableCORS = true

[browser]
gatherUsageStats = false
